<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.ocha.cardproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CardDetailActivity"></activity>
        <activity android:name=".RegisterNewActivity" />
        <activity android:name=".ViewCardsDetActivity" />
        <activity
            android:name=".RegisterExistedPersonActivity"
            android:label="@string/title_activity_register_existed_person"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewCardsActivity"
            android:label="@string/title_activity_view_cards"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CardActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>